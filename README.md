# Sushi Tece 🍣

Современный сайт доставки суши, роллов, наборов, бургеров и напитков.

## Технологии

- **Next.js 14** (App Router) - React фреймворк для быстрой и SEO-оптимизированной разработки
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS** - Utility-first CSS фреймворк
- **React Context API** - Управление состоянием (язык, корзина)

## Функции

- ✅ Локализация на 3 языках (Русский, Украинский, Турецкий)
- ✅ Фильтрация по категориям (Суши, Роллы, Наборы, Бургеры, Напитки)
- ✅ Поиск по меню
- ✅ Корзина с сохранением в localStorage
- ✅ Бейджи товаров (Хит сезона, Новинка, Острое, Вегетарианское, Скидка)
- ✅ Рекомендации к заказу
- ✅ Автоматическое добавление приборов
- ✅ Модальные окна для оформления заказа
- ✅ Адаптивный дизайн
- ✅ SEO оптимизация

## Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Запуск продакшен версии
npm start
```

Сайт будет доступен по адресу [http://localhost:3000](http://localhost:3000)

## Структура проекта

```
sushi-tece/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # Основной layout
│   ├── page.tsx           # Главная страница
│   └── globals.css        # Глобальные стили
├── components/            # React компоненты
│   ├── Header.tsx         # Шапка сайта
│   ├── Footer.tsx         # Футер
│   ├── Cart.tsx           # Корзина (боковое меню)
│   ├── MenuGrid.tsx       # Сетка меню
│   ├── Promotions.tsx     # Блок промо-акций
│   ├── OrderModal.tsx     # Модальное окно заказа
│   └── ConfirmationModal.tsx # Модальное окно подтверждения
├── contexts/              # React Context
│   ├── LanguageContext.tsx # Контекст языка
│   └── CartContext.tsx    # Контекст корзины
├── data/                  # Моковые данные
│   ├── products.ts        # Товары
│   ├── promotions.ts      # Промо-акции
│   └── utensils.ts        # Приборы
├── lib/                   # Утилиты
│   └── i18n.ts            # Локализация
└── types/                 # TypeScript типы
    └── index.ts           # Основные типы
```

## Особенности реализации

- **Корзина**: Данные хранятся в localStorage, не теряются при перезагрузке страницы
- **Локализация**: Язык сохраняется в localStorage, определяется по браузеру при первом посещении
- **Приборы**: Автоматически добавляются в корзину при добавлении соответствующих блюд (палочки для суши/роллов, вилка для бургеров)
- **Рекомендации**: Товары с рекомендациями показывают предложения в корзине
- **SEO**: Метатеги, семантическая разметка, оптимизация для поисковых систем

## Настройка Telegram уведомлений

Для получения уведомлений о заказах в Telegram необходимо настроить переменные окружения:

1. **Создайте Telegram бота:**
   - Напишите [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте команду `/newbot` и следуйте инструкциям
   - Сохраните полученный токен бота

2. **Узнайте свой Chat ID:**
   - Напишите боту [@userinfobot](https://t.me/userinfobot)
   - Он вернет ваш Chat ID (число, например: `123456789`)

3. **Создайте файл `.env.local` в корне проекта:**
   ```env
   BOT_TOKEN=your_bot_token_here
   ADMIN_IDS=[123456789,987654321]
   ```
   
   **Важно:** `ADMIN_IDS` должен быть JSON массивом. Если у вас один администратор, используйте формат `[123456789]`. Для нескольких администраторов: `[123456789,987654321,111222333]`

4. **Перезапустите сервер разработки** после добавления переменных окружения

После настройки при каждом заказе с сайта администратору будет приходить уведомление в Telegram в формате:

```
🆕 Новый заказ!

👤 Клиент:
   ID: 79180200508
   Имя: Sergey
   Телефон: 79180200508

📦 Заказ:
   • Del mare x1 = 450 TL
   • Фила в икре x1 = 360 TL

💰 Итого: 810 TL

🆔 ID заказа: #1234567890
```

## Деплой на Render.com

Для деплоя этого приложения на Render.com необходимо выбрать **Web Service** (не Static Site), так как приложение использует API routes (`/api/order`), которые требуют Node.js runtime.

### Инструкция по деплою:

1. **Подключите репозиторий к Render:**
   - Зайдите на [render.com](https://render.com)
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub/GitLab репозиторий

2. **Настройки сервиса:**
   - **Name**: `sushi-tece` (или любое другое имя)
   - **Environment**: `Node`
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
   - **Plan**: Free (или выберите платный план)

3. **Настройте переменные окружения:**
   В разделе "Environment" добавьте:
   - `NODE_ENV` = `production`
   - `BOT_TOKEN` = ваш токен Telegram бота
   - `ADMIN_IDS` = JSON массив Chat ID администраторов (например: `[123456789,987654321]`)

4. **Деплой:**
   - Render автоматически обнаружит файл `render.yaml` и применит настройки
   - После первого деплоя сайт будет доступен по адресу `https://sushi-tece.onrender.com` (или вашему кастомному домену)

### Важно:
- ⚠️ **Не используйте Static Site** - это не сработает из-за API routes
- ✅ **Используйте Web Service** - это правильный выбор для Next.js с API
- 🔄 При каждом push в main ветку Render автоматически пересоберет и задеплоит приложение

## Будущие улучшения

- [x] Интеграция с Telegram для уведомлений о заказах
- [ ] Интеграция с бекендом
- [ ] Система авторизации
- [ ] Программа лояльности
- [ ] История заказов
- [ ] Реальные изображения товаров
- [ ] Онлайн оплата